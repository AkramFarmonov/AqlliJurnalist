REPLIT AI UCHUN PROMPT (7-BOSQICH - 2-QADAM: MA'LUMOTLAR BAZASINI TO'LDIRISH)
The content generation test was a success. Now, we need to populate the database with an initial batch of 15 real articles. To do this, you will create and run a database seeding script.

Create the Seeder Script: Create a new script file at server/src/scripts/seed.ts.

Write the Seeding Logic: Populate seed.ts with a script that performs the following:

Imports the Drizzle database instance, the articles schema, and the necessary AI services (gemini.ts, etc.).

Defines an array of 15 diverse and interesting topics in Uzbek. Examples: "Kvant kompyuterlarining kelajagi", "Toshkentda elektromobillar infratuzilmasi", "O'zbekistonning IT eksport salohiyati", "Marsni o'zlashtirish missiyalari", "Blokcheyn texnologiyasi va uning qo'llanilishi", "Startap ekotizimini rivojlantirish", "Kibertxavfsizlikning zamonaviy tahdidlari", "Bulutli texnologiyalar biznes uchun", "Mobil ilovalar bozori trendlari", "Raqamli marketingdagi yangi vositalar", "O'zbekistonda turizmni rivojlantirish istiqbollari", "Zamonaviy ta'limda texnologiya o'rni", "Energiya samaradorligi va yashil texnologiyalar", "Robototexnika va avtomatlashtirish", "Genetik muhandislik yutuqlari".

Writes a main async function that iterates through this array of topics.

Inside the loop, for each topic:

Call your AI services to generate a complete article object (including title, full content, category, and tags).

Use the Drizzle ORM db.insert(articles).values(...) method to save the newly generated article object into the database.

Log a success message to the console for each article, like console.log('✅ Successfully generated and saved article: [article_title]');.

Wrap the main loop in a try...catch block to handle potential errors during generation. Log any errors to the console.

At the end of the script, call the main() function to execute it.

Create an npm Script: Modify the server/package.json file to add a new script named "db:seed" that executes this file. The script should be: "db:seed": "tsx src/scripts/seed.ts".

Run the Seeder: After creating the file and the npm script, execute the seeder from the shell by running npm run db:seed from within the server directory.

Show me the full, final console output from the seeding process.

Keyingi qadam:

Ushbu skript ishga tushirilgach, bizning bo'sh veb-saytimiz nihoyat 15 ta professional, AI tomonidan yaratilgan maqolalar bilan to'la, jonli platformaga aylanadi. Bu bizga 8-bosqich — UI/UX'ni sozlash uchun mukammal zamin yaratadi. Natijalarni kutyapman!